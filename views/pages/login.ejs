<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: Verdana, Geneva, Tahoma, sans-serif;
      }
      body {
        padding: 1rem;
      }
      :root {
        --theme: rgb(47, 144, 255);
      }
      .errorMessage {
        background: #bc3939;
        color: white;
        padding: 1rem;
        margin: 1rem 0;
        border-radius: 0.5rem;
      }

      form {
        width: fit-content;
        background: #140e24;
        color: white;
        padding: 1rem 4rem;
        border-radius: 0.5rem;
        margin: 1rem auto;
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      form div {
        padding: 1rem 0 0 0;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }
      form div input {
        padding: 0.5rem;
        background: #ffffff;
        color: var(--theme);
        border: 1px solid var(--theme);
        outline: none;
        border-radius: 0.2rem;
        transition: all 0.2s ease;
      }
      form div input:focus {
        border: 1px solid rgb(42, 237, 91);
      }
      form button {
        padding: 0.5rem 2rem;
        width: fit-content;
        margin: 1rem 0;
        background: var(--theme);
        border: 1px solid white;
        color: white;
        cursor: pointer;
      }
      .linkDiv {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        color: var(--theme);
      }
      .linkDiv a {
        color: var(--theme);
      }
    </style>
  </head>
  <body>
    <h1>Login</h1>
    <% if (locals.message) { %>
    <p class="errorMessage"><%= locals.message %></p>
    <% } %>
    <form action="/user/login" method="post">
      <div>
        <label for="email">Enter Your Email</label>
        <input type="email" name="email" id="email" required />
      </div>

      <div>
        <label for="password">Enter Your password</label>
        <input type="password" name="password" id="password" required />
      </div>

      <button type="submit">Login</button>
      <div class="linkDiv">
        Do'nt have an account?
        <a href="/signup">sign up</a>
      </div>
    </form>
  </body>
</html>
